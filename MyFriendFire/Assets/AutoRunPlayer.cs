using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AutoRunPlayer : MonoBehaviour
{
/*
    GameObject ATree;
    public float TargetDistance;
    public float AllowedDistance = 200;
    //public GameObject TheNPC;
    public float FollowSpeed;
    public RaycastHit Shot;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        ATree = GameObject.FindWithTag("tree");
        transform.LookAt(ATree.transform);
        if (Physics.Raycast(transform.position, transform.TransformDirection(Vector3.forward), out Shot))
        {
            TargetDistance = Shot.distance;
            if (TargetDistance <= AllowedDistance)
            {
                FollowSpeed = 1.0f;
                transform.position = Vector3.MoveTowards(transform.position, ATree.transform.position, FollowSpeed);

            }
            else
            {
                FollowSpeed = 0;
            }
        }
    }*/
}
